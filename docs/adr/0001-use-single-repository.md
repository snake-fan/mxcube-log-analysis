# Use a Single Repository

This project keeps the FastAPI backend, React frontend, LangGraph diagnosis agent, documentation, scripts, and deployment support in one repository. The early product risk is stitching Error Event intake, Diagnosis state, log evidence, retrieved knowledge, and follow-up conversation into one coherent workflow, so a single repository keeps contracts and domain language close while the system is still taking shape.
